/* Generated id = <%= uuid %>, version:<%= version %> */
import { <%=  usecaseClassName %> } from "./usecase";

describe("<%= usecaseName %>", () => {
  let usecase: <%= usecaseClassName %>;

  beforeEach(async () => {});

  it("OK", async () => {
    //fakes
   
    //act
    // usecase = new <%= usecaseClassName %>(); 
    // let result: any = await usecase.execute(); 

    //assert
    // expect(result).toNotBe(null); 
  });

});
